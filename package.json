{
  "name": "raminfo",
  "version": "0.0.1",

  "scripts": {
    "test": "node node_modules/istanbul/lib/cli.js cover node_modules/mocha/bin/_mocha && npm run coveralls",
    "coveralls": "node node_modules/coveralls/bin/coveralls.js < coverage/lcov.info"
  },

  "dependencies": {
    "ezlog": "0.0.10"
  },

  "devDependencies": {
    "mocha": "*",
    "istanbul": "*",
    "coveralls": "*"
  },

  "repository": {
    "type": "git",
    "url": "https://github.com/opensoars/raminfo"
  },

  "readmeFilename": "README.md"
}